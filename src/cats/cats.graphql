interface Somebody {
    name: String!
    pets: [Pet]!
    friends: [Somebody]!
    cat(id: ID!): Cat
}

type Cat implements Pet {
    id: ID!
    name: String!
    age: Int
    mice: Int
}

type Query {
    cats: [Cat]!
}